--- origsrc/libfm-qt-0.11.1/src/CMakeLists.txt	2016-09-23 17:49:17.000000000 -0500
+++ src/libfm-qt-0.11.1/src/CMakeLists.txt	2016-11-06 21:54:34.236649000 -0600
@@ -163,6 +163,8 @@ install(TARGETS ${LIBFM_QT_LIBRARY_NAME}
     DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     EXPORT "${LIBFM_QT_LIBRARY_NAME}-targets"
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     PUBLIC_HEADER
     COMPONENT Runtime
 )
